# Sao chép thành .env cho `npm run dev` (Vite đọc file này, không đọc .env ở gốc)

# Backend mà dev server proxy tới. Trong Docker Compose dùng http://backend:8000
VITE_API_TARGET=http://localhost:8000
